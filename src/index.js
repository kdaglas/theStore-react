import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
  return (
    <div>
      <h1>theStoreapp in React/Redux.</h1>
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('app'));
